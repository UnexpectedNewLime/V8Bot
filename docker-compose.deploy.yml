services:
  car-watch-bot:
    image: ${IMAGE_TAG:-ghcr.io/unexpectednewlime/v8bot:latest}
    container_name: car-watch-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/car_watch_bot.sqlite3
    volumes:
      - ./data:/data
