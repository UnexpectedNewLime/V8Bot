"""Digest domain helpers placeholder."""


def build_digest_title(watch_name: str, listing_count: int) -> str:
    """Build a deterministic digest title."""

    return f"{watch_name}: {listing_count} new listings"
